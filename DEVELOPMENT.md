# 开发约定

## 当前实现边界

仓库已完成 **M1：Task-first 领域与入口基础**、**M2：Task Runtime 与多 Space Retrieval**、**M3：Engineering Graph** 和 **M4：Direct Checkpoint / Candidate Review**。ADR-0003 明确取代此前的 Capture 选择链路与客户端生命周期参数：

- `WorkingIntentSnapshot` 是当前 Task 的非事实工作理解，不包含 Space 路由；每次不可变版本由 `TaskIntentRevision` 表达。Task 与 Space 的相关性由独立的 `TaskSpaceAssociation` 表达，并允许 `0..N` 个结果。
- 不存在 Workspace-to-Space 绑定类型、全局 Active Space、对应配置或绑定命令。
- 显式 `context_search` 可以用 `space_ids` 做硬过滤；只读 `task_context` 只接受 external Session locator、token budget 和 max spaces，不接受 Intent、Signals 或任何 Space/Workspace 路由。
- `task_intent_update` 是 Working Intent 的唯一公开写入口并返回更新后的 TaskContextPack；`task_signal_supersede` 是 Signal 失效入口。
- `TaskContextPack` 同时报告当前 Context Tree/Projection Generation 与可选的历史 Graph Context Tree/Artifact Generation；二者允许不同，且每个 Context 都链接到 Association、typed RetrievalPath 和明确 safety source。
- 同一 Workspace 下的 external Session 只持有各自的 TaskSession、`TaskIntentRevision` 链与非定位 TaskSignals。Graph 查询通过本次请求的 `ResolvedFocus` 精确匹配显式 Graph build 中的 RepositoryId + 完整 ArtifactLocator；Focus 不进入 Runtime，也不跨请求、重启、Task 切换或压缩恢复。
- Workspace 不形成 Space prior。RepositoryId 是团队显式约定的 exact-case 可读名称；`config.toml` 中的本机 Repository Catalog 只权威绑定该 ID 与 `0..N` 个 canonical checkout/worktree。路径、basename、remote、Git common-dir 或共同父目录都不创建或合并身份。
- `state/maintenance.lock` 是安装级最外层门禁：业务 CLI、Hook 和每次 MCP tool call 持共享锁，Setup/Upgrade/Uninstall、知识删除与 reset 持排他锁；冲突非阻塞返回 typed `maintenance_busy`，Hook neutral，锁顺序固定为 maintenance 后进入现有组件锁。
- `data reset` 在 `backups/reset-<id>/new` 构造空 Git/Catalog/Index/Runtime/Engineering/Registry 后，以 `state/reset-journal.json` 驱动同文件系统 rename；5 个 crash seam 的下一次 reset/setup 会先回滚完整旧态。成功保留 old backup 与安装/Agent/Skill/logs，活动知识仓不保留 remote，任何远端 ref 都不修改。
- 首次 `setup --knowledge-store-url` 只接受无内嵌 secret 的非空远端 Store：系统 Git 在事务目录 clone，严格验证 committed Event/Object、Reducer 与 scratch Index 后才 rename 到活动 `repository`。安装清单只保存 remote transport、default branch、stable installation ID、`shared-context/<installation-id>` work branch 和 URL digest；Setup 不 push，Hook/MCP/业务 CLI 只 `open_existing`，不 init/clone 或访问网络。
- `knowledge sync` 在安装级排他门禁内显式 fetch `origin/<default>` 与可选的远端 InstallationWorkBranch，合并后验证 append-only、Event/Object、Reducer 和 Index；冲突恢复原本机 HEAD，push race 最多重试 3 次。唯一 push refspec 固定为 `HEAD:refs/heads/shared-context/<installation-id>`，禁止默认分支、force 和 delete；Hook/MCP 不触发同步。
- 最终团队共享 oracle 固定在 `fixtures/team-sharing/team-sharing-v1.json`：两个独立 HOME/root 以同一 `FE` 和不同 checkout 执行 A write/sync→人工 default merge→B sync/Graph Focus→B reset。Expected 是手写语义输入，不从 production 输出反算；必须同时证明 B 本地路径命中、安装字节保留、活动状态为空和远端 refs 不变。
- SessionStart 在模型推理前同步读取本机 Catalog，并以 non-blocking try-lock 解析或复用 exact locator 的 `AuthorizedSessionScope`。Direct/显式 Group 才返回 bounded activation marker，由 `shared_context_activation_marker(agent, external_session_id)`（`crates/agent-adapter/src/lib.rs`）渲染，硬上限 `SHARED_CONTEXT_ACTIVATION_MARKER_MAX_BYTES = 512` bytes；marker 形状固定，只有 `agent_kind` 与 Hook 事件里的 host session id 随会话变化（`<shared-context-active external_session_id="…">…call task_intent_update with agent_kind "codex" and external_session_id "…" (copy it verbatim; never invent one).</shared-context-active>`），让模型只能照抄真实 session id、不再从文档示例里编造；host session id 不满足 `is_quotable_session_id`（非空、≤128 字节、仅 ASCII 字母数字与 `-_.:`）时，marker 退化为不带 `external_session_id` 属性、改为口头描述"用 `$CODEX_SESSION_ID` 或 Cursor 会话 id"的备用形，绝不截断或转义一个不可信 id 后仍然照抄。Disabled、Catalog/lease 锁忙或异常返回 neutral。PromptSubmit 不重复 marker，也不访问 Runtime/Search。
- Enabled PostTool Hook 不运行 Git、Scanner、Graph rebuild；PostTool 默认不发起 Graph 查询；显式开启 `hooks.artifact_focus_reminder` 后只做只读 `engineering.sqlite` 查询并输出 bounded 提示，仍不运行 Git/Scanner/rebuild、不写事实。它只把安全的机械工具分类合并成非事实、非定位的 TaskSignals。TaskSignal 可影响 Working Intent retrieval，但不是工程 Evidence；工作 Agent 必须直接撰写高价值 Claim Evidence。
- Engineering Graph 是围绕已有 Context/EngineeringReference 的稀疏历史知识快照，不是全仓代码搜索引擎或当前 Context Store 镜像。Builder 只固化 Reference roots 与最多两跳的 build-time ContextRelation closure；`association_rebuild` 按 RepositoryId 分组并去重 Reference 的精确 repo-relative path，Scanner 只读取该有界计划。
- Graph Builder 固化 immutable ContextId+RevisionId、关系 target Revision 和 build-time safety；未接受、Evidence 不完整或冲突 Revision 永不自动注入。`context_tree_oid` 只解释构建来源，Tree mismatch、新 Revision、Withdraw 或无关 append 都不关闭或隐式重建旧 Graph。
- 公开只读 `task_artifact_focus` 接受 Session locator、Intent CAS、absolute path 和无 path 的 kind-specific coordinates；服务端钉定 immutable ActiveTask snapshot，经 Catalog 补全本次 `ResolvedFocus` 并直接返回即时 Pack。它不创建 ID、生命周期、Intent Revision 或 Runtime 记录；missing declared tail 可安全映射，exact Graph 不可达则返回 budgeted `artifact_not_reachable_in_graph`，不 scan/rebuild、不猜测。
- 受限多语言 Scanner、持久 Engineering Reference、kind-specific 确定性 ArtifactLocator、可重建解析投影、ContextRelation 1–2 跳和 Graph RetrievalPath 已通过固定 oracle 与显式 MCP/CLI 工作流验收；move/rename 直接变为 missing，不执行关联猜测。新建或其他 untracked 文件不进入 Graph（#150 的已确认边界），Task query 也不触发 Repository scan。
- `runtime.sqlite` schema 13 持久化 server-owned Task/Intent、WorkEpisode、AgentCheckpoint、内容寻址 operation receipt 与 Candidate Build outbox。Setup/Upgrade 对已知 schema 11/12 直接丢弃本地 Runtime 与 sidecars 后重建；unknown/future schema fail closed，后续失败必须逐字节回滚。
- 公开 `task_checkpoint` MCP/CLI 顶层字段严格等于 `agent_kind`、`external_session_id`、`claims`、`unknowns`。Claim 严格包含 `context_kind`、`statement`、`rationale`、`conditions`、`evidence`；Evidence 严格包含 `evidence_type`、`summary`、`limitations`；Unknown 严格包含 `statement`、`blocking`。
- Runtime 根据 locator 解析当前 Task/Intent，生成并关闭 Episode，以 TaskSession/Task/Intent + canonical Claims/Unknowns 派生稳定 operation key，在一个 SQLite 事务中持久化 Checkpoint receipt 与 Build outbox。调用方不提交 Task/Intent/Episode/boundary CAS、transport key 或生成 ID；同 scoped content 重试返回同一 operation/Checkpoint/Episode/Build/Submission identities。
- Checkpoint ACK 与 same-content replay 都不写 Git；ACK 只表明 outbox 已 durable queued。`candidate_list` 做 bounded/fair recovery，`candidate_get` 做 target-aware recovery；恢复可追加供 Review 使用的 untrusted Candidate submission facts，但 `candidate_confirm` 前不得存在 accepted Context revision/association/publication/confirmation facts。
- Agent 直接提交的 Evidence 只是 attestation，直到用户显式确认 Candidate 才能成为可信 Context。Hook guidance、TaskSignals、Prompt、raw command/tool output 都不能成为 Claim Evidence；空 Claims+Unknowns 是显式 mutation-free `no_op`。
- Candidate Builder 读取 exact closed Episode 和 final Checkpoint，逐 Claim 组装草稿；无 Claim或 Unknown-only 为零 Candidate。Candidate 创建、分析、Review 与 Confirmation 继续保持 server-owned identity、幂等恢复和显式人工治理。
- #159 Candidate Analysis 是 Runtime derived review state：Search 以完整草稿等值、显式 related Context、exact Artifact Graph、topic/scope 与 BM25 多路 RRF 生成 typed assessment，明确区分 exact duplicate、supports、revises、potential contradiction、unresolved related 和 novel。Space 推荐融合 assessment target、source Task association 与 Space Intent；冲突 Intent/unsafe Context 不自动成为 Primary，无安全 Primary 时给出完整 system-suggested Space Intent。分析不写 Git、不进入 Search/Hook/自动注入；`candidate analyze` 可重跑并替换当前结果。
- #161 已定义 CandidateConfirmation 与 ContextSpaceAssociation 的严格 Event/Reducer/Index 事实：确认引用 exact Candidate/source、Primary/Related、结果 Revision、initial Association、causal Publish Event 和 final content hash；后续 Withdraw 不反向抹除历史确认。Association 独立成可修订 DAG，多 Head 与重复 Confirmation 都显式 conflict；当前嵌套 Context owner 与 Search ranking 不变。
- 无 Space 的 `ContextCandidate` 领域类型已经存在；只有 closed WorkEpisode 的 Candidate Builder 可调用内部 #117 submission service，Builder Candidate 仍不可自动注入。
- 既有 Git Writer、事件校验、SQLite 投影、Context 生命周期、CLI/MCP、Agent Adapter、安装器和 NPM 分发能力继续作为 M1 的基础设施。

Repository 范围推理前准入（Mew #181–#189）已接入 Hook：产品私有 Catalog 支持显式 `RepositoryGroup` 管理与漂移修复，纯 `ScopeResolver` 只把注册 checkout 内目录判为 `Direct`、显式 Group exact root 判为 `Group`，其他目录判为 `Disabled`；`AuthorizedSessionScopeStore` 以 locator digest 文件名保存短期、Catalog-bound、无业务正文的 typed lease。SessionStart 只让 Missing locator 解析 cwd 并先持久化决定，Current 直接复用，Stale/Expired/锁忙/异常立即 Disabled；同一 locator 的首次成功决定不会被后续 SessionStart cwd 改写。Enabled marker 只在 startup/resume/compact 的 SessionStart 边界出现并携带本会话的 external_session_id，PromptSubmit neutral；PostTool 只合并安全的非事实 TaskSignals，SessionEnd 删除 exact lease。解析与 lease 热路径不运行 Git 或 Repository scan，不需要 launcher，也不写业务仓库配置。固定 `repository_scoped_activation_acceptance` 使用手写隐私安全 oracle 和文档化 Codex/Cursor payload 验收完整生命周期。

Mew #191 已在 MCP Server 实现基于 current Enabled `AuthorizedSessionScope` 的 Session-level authorization guard：Disabled/Missing/Expired/Stale/busy/corrupt Session 调用被拒绝，Enabled Session 可以显式调查任意已登记 Repository，并可在不伪造 Artifact identity 的前提下保留自包含的非定位工程 Evidence。Mew #192 把全局 Skill 拆为最小 activation gate 与 installer-owned 完整 workflow reference：没有可信 Hook marker 的自动路径不读取 reference、不产生 Shared Context MCP 调用提示；有 marker 才完整读取一次 workflow。Server guard 保证安全和不落越权数据，Skill gate 负责模型推理前的指令准入，两者不能互相替代。

Mew #193 的手写 oracle `fixtures/m5/repository-scoped-context-v1.json` 与真实 `sctx hook`/public MCP wire harness 继续验证 Codex Direct、Cursor explicit Group 和 Disabled 边界。Mew #226–#228 已明确取代其中旧的 Checkpoint/Capture payload 事实；当前链使用 direct Checkpoint→queued ACK→Candidate list/get recovery，Candidate 保持 Pending。该 oracle 只测确定性 instruction/call/result bytes proxy，不是模型 token、供应商计费或模型循环结果。

工作分支 `feat/association-repair` 正在修复"沉淀的知识召回不到、注入 payload 过重"的问题；以下条目描述该分支**当前已经落地**的行为，尚未合并到 `main`：

- `context_search` 与自动检索的显式 `search` 默认 `match_mode: "ranked"`：任意 query token 命中即可入选，排序综合 BM25 与 query token coverage（命中 token 数 / query token 数，`coverage_basis_points`），coverage 低于 `RANKED_MIN_COVERAGE_BASIS_POINTS`（25%）的结果被截断；旧的"全部 token 必须命中"行为保留为 `match_mode: "exact"`，CLI 对应 `sctx search --exact`。自动检索的 `out_of_scope` 排除逻辑改为仅当 query token 未同时命中同一 Space Intent 任一正向字段（`title`/`problem`/`desired_outcome`/`in_scope`/`acceptance_conditions`/`domain_terms`）时才算被排除。检索结果 item 的 `title` 来自 Context 自身 `statement` 的前 60 字符（超出截断并加省略号），`space_title` 是独立字段，不再借用 Space 标题当作 Context 标题。自动检索的 query token 筛选（`automatic_eligible_query_tokens`）按 IDF（文档频率越低优先）排序，`AUTOMATIC_HIGH_DF_MIN_DOCUMENTS` 降到 5；只有索引文档数低于该阈值时才退回内置中英文停用词表兜底。
- 路径/标识符引用派生已从 Checkpoint ACK 移到 Candidate Build 阶段：`task_checkpoint` 本身只做 receipt+outbox（见下一条），不在 ACK 里解析引用。`build_closed_episode`（`crates/mcp/src/lib.rs`）先调用 `Runtime::derive_episode_claim_references`，它经 `TaskRuntime::pending_claim_reference_candidates` 判断该 Episode 是否已经派生过；未派生时才用纯函数从 Claim 的 `statement`/`rationale`/`evidence.summary` 里确定性抽取路径与 CamelCase/snake_case 标识符候选，解析为 `EngineeringReferenceDraft`（`crates/task-runtime/src/reference_derivation.rs`）：含目录的路径走既有 `resolve_declared_path`；只有 basename 的候选，在 Session 已解析出 checkout 时用一次只读、2 秒超时的 `git ls-files` 查找，**只有唯一命中**才解析（歧义或零命中都不解析，reference 的 `limitations` 标 `derived_from_unique_basename`）。`TaskRuntime::derive_episode_claim_references` 在一次 SQLite 事务里把解析结果写回持久化的 Checkpoint Claims，并在标记表 `checkpoint_reference_derivation` 记一次——**第一次决定持久**：之后任何一次 Build 重跑（Hook 恢复、`candidate build-closed-episode` 等）都直接复用这个持久化结果、不再调用 `git`，一次 Episode 的 `git ls-files` 结果只查一次。Repository/Group 未解析、`git` 不存在、查询超时或失败都不会让 Build 失败——未解析的候选进入 `unresolved_hints`，只作为可检索文本，绝不成为图事实。`topic_key_hint` 取解析成功引用中出现次数最多（并列取字典序最小）的 `"{context_kind}:{repository_id}:{path}"`；未解析成功任何引用时为空。同一次 `build_closed_episode` 里，`record_episode_context_usage`（比较本 Task 被注入过什么、Claim 又主张了什么，见下面 WP-H 一条）同样在这个阶段跑，也是读检索索引的 best-effort 派生，不属于 ACK。这条派生链路是同内容重跑幂等的纯函数 + 只读查询组合。
- Candidate Build 在把 Claim 变成候选前，与同一 Task 已存在的 Pending/Confirmed 候选、以及归一化 token 集做 Jaccard 相似度比较（`CANDIDATE_DUPLICATE_SIMILARITY_BASIS_POINTS = 8000`，即 ≥0.8 视为同一事实）：命中时不新建候选，只把新 Evidence 追加到既有候选，保持 same-content replay 返回相同 identities 的幂等语义。
- `candidate_confirm` 与 `candidate_discard` 都支持批量：`candidate_ids` 数组与单个 `candidate_id` 二选一。`candidate_discard` 的批量提交是原子的（一次事务，任一失败整批回滚）。`candidate_confirm` 的批量现在也是**单个 Git batch**：在第一次 Git 写入前对每个 Candidate 做完整校验（Session/CAS/Review 版本/Space），任一校验失败整批不写入并指出具体是哪个 `candidate_id`；校验全部通过后，`GitStore::confirm_candidates`（`crates/git-store/src/store.rs`）用一把 Confirmation 锁、一个 Writer batch、一次 `commit_journal_locked` 提交整批 Candidate 的 Event（对批内已经以相同语义确认过的成员按单条 replay 语义返回 `AlreadyExists`、不重复写入，不影响同批其余新成员一起写入同一个提交），随后 `TaskRuntime::finalize_candidate_confirmations` 在一次 SQLite 事务里为整批落地 Review 终态。批量模式下不支持 `edits`，也不支持新建 Space 的 `proposed` 推荐（只能确认到已有 `existing_space_id`），违反时返回 typed 错误。
- `task_intent_update`/`task_context`/`task_artifact_focus`/`candidate_list` 支持 `detail_level: "compact" | "full"`：MCP 侧默认 `compact`；CLI 直接 Rust 入口（`task_context_readonly`、`candidate_list` 等无 `_with_detail` 后缀的函数）默认仍是 `full`，与既有行为保持一致。`compact` 的预算选择是独立计算的（不是把 `full` 结果截断投影），目标是同一 2000 token 预算下能装下更多条目：Context item 在打包时优先，最多用掉 `COMPACT_ITEM_BUDGET_BASIS_POINTS = 7_000`（70%）的 token 预算，且 `COMPACT_MIN_ITEMS = 3` 条 item 永远不会因为超预算被丢弃（`crates/search/src/lib.rs`）；每条 Context 只保留 `context_id`/`revision_id`/`space_id`/`kind`/`status`/`title`/`statement`/`applicability.conditions`/最多 200 字符的 `evidence.summary`/`relations`/去重后最多 `COMPACT_RETRIEVAL_CHANNEL_LIMIT = 6` 个 `retrieval_channels` 名字/最多 3 条 `why`，去掉 `match_reason` 数值、完整 `retrieval_paths` 载荷、`safety_source`；顶层 `candidate_spaces[].reasons` 只保留可读句子（`CompactSpaceAssociation` 只含 `space_id`/`title`/`score`/`provisional`/`reasons` 五个字段，`crates/search/src/lib.rs`），`retrieval_paths` 数组只出现一次；`omitted` 每条给出 `context_id`/`title` 而不是空计数。`candidate_list` 的 compact 视图每条只有 `candidate_id`/`kind`/`statement`/`top_assessment`（最高置信度的 `relation` + `confidence_basis_points`）/`primary_space_recommendation`/`ready_for_review`。CLI 新增 `sctx task context --compact` 与 `sctx candidate list --compact`，映射到新增的 `sctx_mcp::task_context_readonly_with_detail_at_root` / `candidate_list_with_detail_at_root`（真正复算 Compact 预算选择，而不是对 Full 结果调用 `.compact()`——后者在 Full 分支下 `compact_items`/`compact_associations` 恒为空）。
- 同一 `external_session_id` 下并发的多个 Agent（例如 fork 出的子 Agent）在 `task_boundary: "continue"` 且 `expected_revision_id` 已过期时，如果新 Intent 的 `goal` 归一化后与当前 Head 不同，服务端会自动开出一个**并行 TaskSession**（`IntentRevisionWriteStatus::Forked` / 响应 `revision_status: "forked"`），而不是返回 `intent_stale`；调用方应把这当作"已被分配到并行 Task"而非错误来处理。
- `ContextRevision`/候选草稿的可选 `problem_view` 字段与 `hints: Vec<String>` 字段已经全链路接通。`context_revision`/`context_candidate` 表与 `context_fts` 都有对应列（`problem_view`、`hint_text`）。Candidate Build 阶段（见上一条）把 `reference_derivation::claim_hints` 算出的未解析路径/标识符拼写直接写进候选草稿的 `hints` 字段（`crates/mcp/src/lib.rs` 构造 `ClaimBuildMaterial` 的 `draft` 处），`context_fts.hint_text` 由它投影而来。`problem_view` 在候选草稿阶段仍是 `None`（Build 没有 Task Intent 可读），改为在 `candidate_confirm` 时按需自动填充：`derived_problem_view`/`compose_problem_view`（`crates/mcp/src/lib.rs`）读取来源 Episode 关闭时所在的 `TaskIntentRevision`，把 goal、`In scope`、`Open questions` 拼成不超过 `MAX_DERIVED_PROBLEM_VIEW_CHARS`（400 字符）的一段话；只有草稿和显式 `edits.problem_view` 都未设置时才写入，显式 `edits.problem_view`（含 `clear`）始终优先。公开 `candidate_confirm` 的 `edits` JSON Schema（`candidate_edits_schema`，`crates/mcp/src/lib.rs`）现在同时接受 `problem_view`（`{"action":"set","value":...}` / `{"action":"clear"}`）与 `hints`（字符串数组）编辑。新表 `token_alias(token, alias, source, group_key)` 从 Reference basename/标识符（`source = identifier_split`）与 Space Intent `domain_terms`（`source = domain_term`）确定性投影生成，可通过事件重放重建；显式 `search` 与自动检索现在都用它做 query token 扩展（见下一条）。BM25 权重 `CONTEXT_FTS_BM25_WEIGHTS = "0.0, 0.0, 8.0, 8.0, 4.0, 2.0, 8.0, 2.0"`（`crates/search/src/lib.rs`，对应 `context_id, revision_id, title, statement, rationale, evidence, problem_view, hint_text` 8 个 FTS 列）已把 `problem_view` 设到与 `title`/`statement` 相同的 8.0、`hint_text` 设到与 `evidence` 相同的 2.0，两者都参与排序。
- 别名驱动的检索扩展已接通：`AliasExpansion::load`（`crates/search/src/lib.rs`）在显式 `search` 的 `ranked` 模式与自动检索（Space Intent 与 Context 两条查询路径都会读它）里把 query token 按 `token_alias` 分组展开成 FTS `OR` 查询；`match_mode: "exact"` 从不展开——代码注释原话是"`exact` is the literal-spelling mode, so it never expands through `token_alias`"。展开受三个常量约束：`MAX_ALIASES_PER_QUERY_TOKEN = 8`（单个 query token 最多拉入的别名数）、`MIN_ALIAS_GROUP_MEMBERS_IN_QUERY = 2`（一个别名组必须已有至少 2 个成员出现在 query 里，才会展开该组其余成员，避免一个常见共享词把整组都拉进来）、`MAX_ALIAS_EXPANSIONS_PER_QUERY = 16`（一次 query 的展开总数上限）；覆盖率仍按原始 query token 计数，别名命中记为它所属原始 token 的一次命中，不会因展开而虚增 coverage。命中结果的 `MatchReason.matched_via_alias` 列出仅通过别名才命中的 query token。Scope 通道（`ScopeTargets`，`crates/search/src/lib.rs`）的 `condition` 维度现在比较归一化 token 集合：Context 的 `condition` 短语要求其每个 token 都出现在 Task `intent.goal` 与 `in_scope` 的归一化 token 并集里才算匹配，不再要求整句字符串相等；`domain` 维度通过 `with_domain_aliases` 额外接受 `token_alias` 展开后的别名拼写相等。
- WP-H 已落地两张本机 Runtime 表（`crates/task-runtime/src/lib.rs`，用 `IF NOT EXISTS` 新增，不进 Git/index）：`task_injection`（`task_id`、`context_id`、`intent_revision_id`、`revision_id`、`injected_at_unix_seconds`、`source`；`source` 限定 `intent_update`/`task_context`/`artifact_focus` 三个检索入口，主键 `(task_id, context_id)`）与 `context_usage`（`context_id`、`task_id`、`outcome` 限定 `reused`/`ignored`/`refuted`、`recorded_at_unix_seconds`，主键 `(context_id, task_id)`；`refuted` 不会被后续写入降级覆盖）。`task_intent_update`/`task_context`/`task_artifact_focus` 三个检索入口都调用 `record_task_injections` 写注入记录；Candidate Build 阶段的 `record_episode_context_usage`/`record_checkpoint_context_usage`（见前面"路径/标识符引用派生"一条）按归一化 statement token Jaccard 比较本 Task 被注入过的 Context 与本次 Checkpoint Claims，得出 `reused`/`ignored`；`candidate_confirm` 确认带 `contradicts` 关系的候选时额外为被反驳的 Context 补记一条 `refuted`。检索排序读这个本机 usage prior（`with_usage_prior`/`apply_usage_prior`，`crates/search/src/lib.rs`）：`reused` 把融合分乘以 `USAGE_REUSED_BONUS_BASIS_POINTS = 11_500`（×1.15），同一 Context 累计 `ignored` 满 `USAGE_IGNORED_PENALTY_MINIMUM_TASKS`（3 次）后乘以 `USAGE_IGNORED_PENALTY_BASIS_POINTS = 9_000`（×0.9）；`refuted` 不在这里重复降权（已有语义冲突降权覆盖，见前面 `CONFLICT_SCORE_MULTIPLIER_BASIS_POINTS` 一条）。`task_context` 的 full item（`ContextPackItem`）新增 `usage: ContextUsageCounts` 字段。这两张表目前没有清理或 TTL（见"尚未实现"）。
- WP-J 已落地系统生成 Space 的 `provisional` 语义：`candidate_confirm` 确认 `proposed` 新 Space 推荐时，新建的 `IntentRevision.provisional`（`crates/domain/src/model.rs`，`#[serde(default, skip_serializing_if = ...)]`，老事件字节不受影响）标 `true`（`crates/domain/src/confirmation.rs`）；人工 `space intent revise` 写的新 Revision 不带这个 flag，这就是 provisional Space 摆脱 provisional 状态的方式。建议的新 Space 标题只取自 Working Intent 的 `goal`（`proposed_space_title`，`crates/search/src/candidate.rs`），去掉历史 `System suggestion:` 前缀、规范空白后按**字符**截断到 `PROPOSED_SPACE_TITLE_MAX_CHARS = 40` 并加 `…`（不是词边界截断，无空格书写的语言也能截得动；goal 为空时回退固定标题 `"Task intent"`）。`provisional` 现在是索引投影列 `space_projection.provisional`（`crates/index/src/schema.rs`），`space_list` 响应体每个 Space 顶层带一个由当前唯一 intent head 算出的 `provisional` 布尔字段（冲突头的 Space 永不报告 provisional），`space get` 直接序列化 Space 投影、`provisional` 出现在其中 `intent.revisions.<revision_id>.provisional`；候选 `space_recommendations` 通过 `insert_space_recommendation_provisional` 带 `provisional`。`candidate_list` 顶层新增 `space_advisories`（`provisional_space_advisories`，`crates/mcp/src/lib.rs`）：当一个 provisional Space 的 accepted Context 数达到 `PROVISIONAL_SPACE_MERGE_THRESHOLD = 5`，或存在指向该 Space 的跨 Space related 引用时，给出人工合并/命名提示。
- `sctx repository doctor` 对 Catalog 中每个 legacy `rpo_<uuid>` 前缀的 RepositoryId 给出 typed 警告（`--json` 下稳定 `kind: "legacy_repository_id"`），并统计本机 Index 里还有多少条 `EngineeringReference` 引用该 legacy id（只读统计，不改 Git）。`sctx repository rename --from <OLD> --to <NEW>` 只改本机 Catalog / repository-registry，`--to` 必须满足 ADR-0001 的 1–64 字节可读 ASCII 语法；目标已存在或来源不存在都返回 typed 错误；已写入 Git 的 `EngineeringReference` 事件里的旧 id 不会被重写。
- **Hook 主动工程图提示：P4.1 实验已实现但默认关闭** — Enabled PostTool Hook 默认只把安全机械工具事件合并成非事实 TaskSignal，不运行 Git、Scanner、Graph rebuild，也不发起 Graph 查询。`config.toml` 新增可选 `[hooks] artifact_focus_reminder`（缺省 false）；关闭时 PostTool 代码路径与开关引入前逐字节一致。显式开启后，Direct 会话里被 `ToolCategory::FileOperation` 归类且只有一个结构化 file hint 的事件，会用 Catalog 把绝对路径解析为 `(RepositoryId, RepoRelativePath)`，以 read-only、`busy_timeout=0`、不设 `PRAGMA journal_mode` 的连接查询 `state/engineering.sqlite`，命中 accepted 且 injection-eligible 的 Graph Context 时输出 ≤ 800 字节的提示（≤ 3 个 `context_id` + ≤ 60 字符标题 + 一句固定 `task_artifact_focus` 文案）。提示不含 statement/evidence 正文、不含 Prompt、不写任何事实；同一 Session 的同一 Artifact 只提示一次（`state/artifact-reminders/<locator-digest>.json`，≤ 64 条 FIFO）；查询超时、投影缺失、写去重状态失败或任何错误都退回与关闭时一致的 neutral 输出。
- `candidate_confirm` 确认带 `contradicts` 关系的候选时，会在同一 batch 里追加 `SemanticConflictOpened`（`contradiction_conflict_openings`/`conflict_already_open`，`crates/mcp/src/lib.rs`），不需要再手动 `sctx semantic conflict open`。但仍受 reducer 的既有投影规则约束（`projectable_conflict_participant`）：目标必须与新 Revision 同一 Space、双方都是 accepted 发布头、都是 `decision`/`contract` 两种 `context_kind`、`topic_key` 相同，且 applicability 的 domains/platforms/conditions 三个维度分别有交集（某维度任一侧为空视为不限制）；任一条件不满足则只落 `contradicts` Relation 本身，既不开冲突也不报错。已有未解决冲突覆盖同一对参与者（按对方 Context 与本次确认内容哈希判断）时不重复开，同内容 replay 幂等。`sctx semantic conflict resolve` 流程不变。
- 未解决语义冲突的参与方仍被 domain 不变量硬排除出**自动注入**（`ContextPackMode::AutomaticInjection` 下任何非空 `conflicts` 直接不返回该 item，`crates/search/src/lib.rs`），响应体的 `auto_injection_eligible` 字段本身不因冲突改写。`CONFLICT_SCORE_MULTIPLIER_BASIS_POINTS = 7_000` 只在这个硬排除不生效的 `ContextPackMode::Explicit` Pack 排序里，把该 item 的融合分乘以 0.7，并在 `derived_state.demotion_basis_points` 上报；显式 `search` 只把冲突事实放进 `conflicts` 字段供 explain，不应用该乘数、`demotion_basis_points` 恒为空。`STALE_SCORE_MULTIPLIER_BASIS_POINTS = 6_000` 是同一降权机制里 `recheck_when` 命中的乘数（见下一条），两个模式的 Pack 排序都会应用，只是不受硬排除限制。
- `ContextRelationKind::Supersedes`（`crates/domain/src/engineering.rs`）是写在新 Revision 上的普通 immutable Relation，不改写目标在 Git 里的既有事实。Index 投影 `refresh_superseded_by`（`crates/index/src/schema.rs`）在全量与增量重建后，把每个被至少一个 accepted Revision `supersedes` 的 Context 的 `context_item.superseded_by` 设为该来源 Context（可跨 Space；多个来源并列时取最小 Context ID）。被替代 Revision 排除自动注入（`ContextDerivedState::blocks_automatic_injection`），仍可被显式 `search`/`task_context` 检索、explain 到，并带 `derived_state.superseded_by`。
- `recheck_when` 里以 `branch_advanced:<branch>@<commit>` 或 `file_changed_since:<commit>:<path>` 开头的条目是服务端可确定性评估的结构化子集，其余仍是自由文本、不评估。评估只在 `sctx doctor --recheck` 与 `association rebuild` 之后触发一次，对每个 accepted Context 用其 Repository 的本地 checkout 只读判断分支是否已前移、文件在两个 commit 之间是否变更过，命中写入本机 `context_item.stale_reason` 投影列（`record_stale_reasons`），不产生 Git 事件、不改 Git 事实。`stale_reason` 是纯本机派生状态：projection 全量重建的 INSERT 语句会把它连同 `superseded_by` 一起硬置回 `NULL`（`crates/index/src/schema.rs`），重建之后必须重新跑一次 `doctor --recheck` 才能恢复。命中项在 Pack 排序里乘以 `STALE_SCORE_MULTIPLIER_BASIS_POINTS`（0.6），并在 compact 视图的 `why` 里给出原因；这条不像语义冲突那样在自动注入里被硬排除，只是降权。
- `config.toml` 可选 `[context_ttl]` 表按 `context_kind` 配置一个带单位（`s/m/h/d/w`）的正时长，例如 `[context_ttl]` 下 `validation = "30d"`、`progress = "14d"`（`crates/local-state/src/config.rs`）。到期判断的锚点是该 Context 被接受时所在 commit 的时间：`accepted_at_unix_seconds`（`context_item` 投影列）取自引入该 Publication 事件的 commit 时间，不是本机写入或查询时间。超过对应 `context_kind` 的 TTL 判定为 `historical`（`ContextDerivedState::historical_reason`）：排除自动注入，`search`/`task_context` 仍可见并带原因。
- Index `DB_SCHEMA_VERSION` 现在是 `"15"`（`crates/index/src/lib.rs`），覆盖本节新增的 `superseded_by`/`stale_reason`/`problem_view`/`hint_text`/`token_alias`/`space_projection.provisional` 等列与表，以及下面 WP-L7 一条描述的 `hint_text`/`identifier_split` 抽取口径变化；`main` 分支上同一常量仍是 `"11"`，即本分支相对 `main` 把 Index schema 从 11 升到了 15，`runtime.sqlite` 的 schema 版本不受影响、仍是本文档前面记录的 13（新增的 `task_injection`/`context_usage`/`checkpoint_reference_derivation` 等表都用 `CREATE TABLE IF NOT EXISTS` 加在 schema 13 里，没有再次 bump `PRAGMA user_version`）。`sctx index rebuild` 与 setup/upgrade 在检测到旧 schema 时整表重建，不是逐字节回滚。
- WP-L7 把投影期的标识符/路径抽取统一到 `sctx_domain::hints`：全量与增量投影都会把每条 revision/候选的 `statement`、`rationale`、每条 Evidence 的 `supports`/`interpretation`/内容字符串叶子一起喂给 `derived_hint_terms`/`derived_path_stems`（`crates/index/src/schema.rs` 里的 `prose_hints`），识别出的全部标识符/路径拼写都并入 `hint_text`；但 `token_alias` 的 `identifier_split` 别名组只从 `prose_hints().alias_seeds`——也就是路径 basename 种子——生成，一句话里偶然出现的 CamelCase 词不会单独开出别名组。检索侧候选分析器新增 `SharedIdentifier` 评估路径（`crates/search/src/candidate.rs`）：`add_identifier_channel` 取 Candidate 与目标 revision 各自 free prose 里的标识符集合求交集，达到 `SHARED_IDENTIFIER_STRONG_OVERLAP = 2` 个才开出这条通道，并且只有交集数在同一 `context_kind` 下才计入 `strong_identifier`；达到 `SHARED_IDENTIFIER_SUPPORT_OVERLAP = 3` 个（或语句相似度已经过审阅阈值）才判定为 `identifier_restates`（记为 `Supports` 而不是 `Revises`）。
- Candidate Build 一次提交多个 Candidate 现在真正走批量 Git 写入：`GitStore::submit_candidates`（`crates/git-store/src/store.rs`）用一把 Candidate Writer 锁、一个 pending journal 和一次 `commit_journal_locked` 提交整批请求的 Event，单批大小上限 `MAX_CANDIDATE_SUBMISSION_BATCH`；每个 Candidate 仍保留自己的 `SubmissionId` 幂等与自己的 Writer batch id，submission 索引因此仍能把每个 Candidate 精确映射回它自己的 Event，同批里已按相同内容确认过的成员按单条 replay 语义返回 `AlreadyExists`、不影响同批其余新成员一起写入同一次提交。MCP 侧的 Build 入口（`crates/mcp/src/lib.rs`）按这个上限把待提交 Claim 分块调用；全部分块提交完之后，只取一次 `self.snapshot()`，对本次新写入的每个 Candidate 复用同一个 `DomainSnapshot` 调用 `analyze_candidate_in_snapshot` 做分析，不再逐个重新打快照。
- WP-L6 给三条重复的 Git/SQLite 读路径加了进程内缓存，均只在同一长驻进程存活期内生效，进程重启即失效、不改变任何持久事实：`ProjectionIndex`（`crates/index/src/lib.rs`）按完整 `IndexMetadata`（含 indexed tree oid 与 schema/实现版本）做键缓存上一次归约出的 `Arc<DomainSnapshot>`（`cached_domain_snapshot`/`publish_domain_snapshot`），metadata 不变就直接复用、不重新走一遍 `build_input`；`event_commit` 表持久化 Event 被引入的 commit 与提交时间，故意不列进 `TABLES`，全量重建的影子表 swap 因此不会清空它，新进程可以直接热启动而不必重新走一遍 history 扫描；HEAD→tree 的解析结果按 `MAX_REMEMBERED_HEAD_TREES = 8` 做有界记忆；一次只读 `git ls-files` 的答案（用于 Checkpoint Build 阶段的路径/标识符解析，`crates/task-runtime/src/reference_derivation.rs`）按 `(git_dir, HEAD, `.git/index` 的 `size:mtime`, patterns)` 做键、进程内 LRU 上限 `MAX_TRACKED_PATH_CACHE_ENTRIES = 32`，索引文件一变化键就自然失效。`introducing_commits`（`crates/index/src/git_tree.rs`）统一带 `--no-renames` 调用 `git log`，避免 rename 检测带来的额外开销和不确定性。上述缓存全部命中时，一次稳态 `build_input` 组合调用最多产生 1 个 git 子进程。
- WP-L8 改了检索 coverage 的口径。显式 `search` 的 `ranked` 模式，覆盖率分母不再是 query 的全部 token 数，而是"这棵 Tree 至少能回答的 query token 数"（`answerable_query_token` CTE，`crates/search/src/lib.rs`）：完全答不出任何一个 revision 的 token 不再拖累分母；`RANKED_MIN_COVERAGE_BASIS_POINTS`（25%）阈值本身不变。自动检索的文本覆盖门槛（`AUTOMATIC_TEXT_COVERAGE_THRESHOLD_BASIS_POINTS = 6_000`）也不变，但标识符 query token 现在按 `AUTOMATIC_IDENTIFIER_TOKEN_COVERAGE_WEIGHT = 3` 倍计入覆盖分子，且只有 query 里的标识符 token 数达到 `AUTOMATIC_MIN_IDENTIFIER_QUERY_TOKENS = 2` 才生效，避免一个偶然的驼峰词单独把整条门槛顶过去。item 排序阶段新增独立的 coverage 加权：`ITEM_TEXT_COVERAGE_SATURATION_BASIS_POINTS = 3_000` 是覆盖率的封顶点，`ITEM_TEXT_COVERAGE_MULTIPLIER_FLOOR_BASIS_POINTS = 5_000` 是覆盖率为 0 时的乘数下限，二者之间线性插值。
- WP-T/WP-L8 的关联召回验收固定成两个手写 probe 集，且都是 **blocking** 测试（不是可选观察用例）：`crates/cli/tests/association_probe_workflow.rs`（`fixtures/association/probe-v1.json`，22 条中英混排 probe：9 条中文改写、5 条英文改写、7 条标识符、1 条噪声，目标 `search_hits`/`intent_hits` 都 ≥ 18/22）与 `crates/cli/tests/association_probe_zh_workflow.rs`（`fixtures/association/probe-zh-v1.json`，24 条以中文为主的 probe：16 条中文改写、4 条标识符、2 条英文、2 条噪声，目标都 ≥ 19/24）。当前两个集合的显式 `search`/自动 `task_intent_update` 命中都是 20/22 与 19/22（英文混排集）、20/24 与 19/24（中文集，代码注释里记为"explicit search 17 -> 20/24"、"automatic 16 -> 19/24"），噪声探针零命中，均已经过关阈值。
- 已评估但未引入 `jieba-rs` 词级分词：两个探针集在改用 `jieba::cut`/`cut_for_search` 后逐条结果零变化（bigram 已经覆盖同样的召回面），且 release 构建下 `Jieba::new()` 初始化本身就要约 128ms，收益不足以抵消常驻开销，因此没有新增这个依赖。"尚未实现"里保留的 Embedding 检索通道（P2.5）是因为剩下的未命中探针（`probe-zh-v1` 里的 zh-01/zh-02/zh-06/zh-07 等改写后与原文本零 token 交集的探针，混排集里也有类似情形）任何词法/别名手段都够不到——它们需要语义层面的相似度，不是再调一个阈值或再扩一层别名能解决的。
- 两个已知性能边界，规模或首次执行相关，不是回归：新写入的 launcher 二进制在 macOS 上第一次被 Hook 拉起时会先付一次 Gatekeeper/代码签名校验（约 0.4–2.5s），同一常驻进程/同一二进制的后续调用落到稳态 6–13ms；这与上面 WP-L6 的进程内缓存无关，是操作系统对新二进制的一次性开销，重启一个已经跑过的二进制不会重新触发。`candidate_list` 在一个约 26 万文件的 monorepo 上，首次调用因为要跑一次 `git ls-files` 大约耗时 0.9s，后续调用命中上面 `MAX_TRACKED_PATH_CACHE_ENTRIES` 缓存后明显更快。

以下能力仍在同一分支**进行中、尚未接通**，不得当作已生效：

- 中文自然语言联想检索（jieba 词级分词等）还没有引入新依赖；已落地的是 token/IDF/coverage 排序与停用词表补充、`token_alias` 别名扩展，不是分词器替换。

以下能力**尚未实现**，不得在代码、测试报告或评审中宣称已经具备：

- **后台同步与 PR 自动化：未实现** — 用户可显式执行 `knowledge sync`，但没有定时任务、托管平台识别或自动创建/合并 PR；Repository Catalog 仍是单机显式配置，不是团队事实。
- **真实 token/物理进程隔离证明：未实现** — #193 已关闭 activation/reference/MCP call/result bytes proxy 与 NPM/分发回归，不得把这些 bytes 当作 tokenizer 输出或真实计费 token。MCP 进程与工具 Schema 仍可能由用户级配置全局启动或可见；Server 拒绝只证明安全，不倒推出调用前 token 节省。
- **Embedding 检索通道：未实现，P2.5 是待决策点** — 当前检索完全基于 BM25 + token coverage + IDF 排序 + 确定性别名投影，没有向量/embedding 索引，也没有引入任何 embedding 模型或服务依赖；是否引入、引入哪种模型、如何保持确定性/可解释仍是未决问题。
- **`applicability_inherited` 逐 Claim 持久化：未实现** — 响应体的 `applicability_inherited` 字段（`crates/mcp/src/lib.rs` 的 `APPLICABILITY_INHERITED`）当前是硬编码常量 `true`，不是按每条 Claim/候选实际判断并落盘的字段；`materialize_direct_checkpoint_claims` 的 applicability 仍整体继承自 Intent。
- **Hook 主动工程图提示只覆盖 File locator 与 Direct 会话：未实现更广范围** — `artifact_focus_reminder_file`（`crates/agent-adapter/src/lib.rs`）只在 `ResolvedActivationDecision::Direct` 且事件恰好含一个 `PathHint::File` 结构化提示时才查询；Group 会话与 `task_artifact_focus` 支持的其他 kind-specific coordinates（Module/Symbol/API/Schema/Test）都不触发提示，不是遗漏而是当前范围边界。
- **`stale_reason` 不是可持久依赖的事实：只是易失投影** — `context_item.stale_reason` 由 `doctor --recheck`/`association rebuild` 写入本机 SQLite 列，但 Index 全量投影重建（`crates/index/src/schema.rs` 的 INSERT）会把它与 `superseded_by` 一起硬置回 `NULL`；重建后必须重新触发一次评估才能恢复，不能假设它在两次重建之间存活。
- **非 `task_checkpoint` 工具仍每次重建 Runtime：未优化** — `authorize_and_call`（`crates/mcp/src/lib.rs`）只让 `task_checkpoint` 复用身份 preflight 已经打开的 `TaskRuntime`；其余每个公开工具调用之后仍会各自 `Runtime::open_with_catalog` 打开一份完整 Runtime（Git/Index/Engineering/Repository Registry 齐开），这是 WP-P 报告过、尚未处理的剩余固定开销，不要把它当作已经和 Checkpoint 一样惰性化。
- **`task_injection`/`context_usage` 无清理或 TTL：未实现** — WP-H 新增的这两张本机 Runtime 表（`crates/task-runtime/src/lib.rs`）只有写入路径（`record_task_injections`/`record_context_usage`），没有过期、清理或容量上限；长期运行会无限增长，不同于 `cleanup_expired_candidate_reviews` 对 Candidate Review 的清理。

Cursor 与 Codex 都通过显式 `task_intent_update` 建立 TaskSession 的首个 `TaskIntentRevision`；SessionStart 的 marker 只声明本地范围已授权并给出本会话的 `external_session_id`，Prompt Hook 不重复提示。MCP `session_not_authorized` 的 message 直接给出恢复线索（照抄 marker 里的 host session id；Codex 也可查 `$CODEX_SESSION_ID`，Cursor 即 conversation id），但不泄露任何已授权 id。Skill workflow 声明知识库正文默认用中文书写（代码标识符/路径/命令保持原文），`candidate_list` 对不含中文的 statement 返回非阻断的 `language_hint`（内容取自常量 `CHINESE_KNOWLEDGE_BASE_HINT`，`crates/mcp/src/lib.rs`）。已有 ActiveTask 可通过只读 `task_context` 再取 Pack。

Mandatory Gate #114/#117 已完成：Builder 的内部 submission service 将稳定 `submission_id` 写入 Git Event 并可重建到 SQLite submission/conflict 索引；Candidate lock 覆盖索引同步、lookup、pending recovery 和 append，主写路径不扫描全量 Event，也不依赖 commit subject。known-v1 malformed Candidate 只有在可安全提取合法 SubmissionId 时形成 submission-local conflict；unknown schema、无 hint 或其他 submission 的坏 Event 只保留 diagnostic。内部 admission 在任何 Git 写入前验证 #156 的 exact closed Episode ownership；不存在手工 MCP/CLI Candidate 创建入口。

#164 固定 oracle 位于 `fixtures/m4/fixed-oracle.json`，由跨层测试直接驱动 Builder→Review→existing/new Confirm，不能从 production 输出生成。Mew #226–#228 显式取代旧 Capture 输入路径；当前 M4 gate 固定运行 direct Checkpoint、queued recovery、Working Intent/Hint、M3 跨端 Graph、真实 Cursor/Codex Hook、privacy、Builder submission、analysis、Review 与 Confirmation 对抗套件。

## Dynamic Replay Phase One（#171）

#176 在 test-only runner 上关闭了第一阶段动态 Replay 验证。六个场景是手写、合成、经隐私审查的协议输入，在隔离 sandbox 中调用真实本机 `sctx`；它们不是对真实用户 Session 的回放。本机真实 Codex/Cursor 数据只用于人工提炼事件密度、相对生命周期顺序、Compaction、缺失 Hook 与并发位置等聚合节奏，不提交 Prompt、transcript、tool output、路径、用户/Session/领域 ID 或逐 Session 事件序列。

默认测试保留六场景各一次的确定性真实执行；6×20 长跑是显式、非阻塞的附加证据，不进入每次提交或 merge gate。Codex `0.147.0` 与 Cursor `3.13.2` 仅是 fixture profiles，不是版本支持矩阵。报告分类、产品修复人工 Gate、延期范围、显式命令和已审计 120-run 聚合见 [`docs/dynamic-replay-phase-one.md`](./docs/dynamic-replay-phase-one.md)。`fixtures/m4/fixed-oracle.json`、`hook_to_confirm_chain`、Rust/NPM workspace gates 与 #150 的 untracked-file 边界继续 blocking，Replay 不得替代或弱化它们。

## 环境与检查

需要 Rust stable（最低 Rust 1.85）、`rustfmt`、`clippy`、Git，以及 Node.js 18 或更新版本。

提交门禁：

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo test --workspace --locked
(cd npm && npm test)
```

定向执行里程碑验收：

```bash
cargo test --locked -p sctx-cli --test milestone_one_contract
cargo test --locked -p sctx-mcp --test mcp_contract
cargo test --locked -p sctx-cli --test milestone_two_contract
cargo test --locked -p sctx-cli --test milestone_three_contract
cargo test --locked -p sctx-cli --test milestone_four_contract
cargo test --locked -p sctx-cli --test hook_to_confirm_chain
cargo test --locked -p sctx-cli --test repository_scope_foundation
cargo test --locked -p sctx-cli --test repository_scoped_activation_acceptance
cargo test --locked -p sctx-cli --test repository_scoped_context_acceptance
```

提交 `Cargo.lock`，确保 CLI workspace 的本地与 CI 构建使用相同依赖解析结果。

## Crate 依赖方向

依赖只能从表格下方向上方流动；`cli` 是组合入口，`domain` 不依赖其他 workspace crate。

| 层级 | Crate | 允许的 workspace 依赖 |
|---:|---|---|
| 0 | `domain` | 无 |
| 1 | `local-state`, `event-schema`, `task-runtime` | `domain` |
| 2 | `git-store` | `domain`, `event-schema`, `local-state` |
| 3 | `index` | `domain`, `event-schema`, `git-store` |
| 4 | `search` | `domain`, `engineering-graph`, `index` |
| 5 | `mcp`, `agent-adapter` | M1 已声明的更低层 crate |
| 6 | `adapter-cursor`, `adapter-codex` | `domain`, `agent-adapter` |
| 7 | `installer` | Adapter、Store、Index、Search 等更低层 crate |
| 8 | `cli` | 所有库 crate |

跨 crate 的可恢复错误统一使用 `sctx_domain::Error` 和 `sctx_domain::Result`。新增依赖时必须保持以上有向无环结构，并同步更新 workspace 契约测试。

## Fixtures 与 Schema

- `fixtures/` 与 `tests/fixtures/` 保存跨 crate 的只读测试输入，不在测试中原地改写；`tests/oracles/` 保存手工编写的固定 expected，不得从生产输出反算。
- `schemas/` 保存随源码版本控制的 Schema。
- `npm/` 保存 NPM launcher、平台包和离线 Bundle 源码。

这些目录不得被 `.gitignore` 整体排除；构建产物应使用精确到产物目录的忽略规则。

## NPM 打包检查

NPM 层没有第三方 JavaScript 运行时依赖：

```bash
cd npm
npm test
```

macOS 测试会为 `aarch64-apple-darwin` 和 `x86_64-apple-darwin` 构建真实 CLI，因此两种 Rust target 都必须安装。arm64 主机执行当前架构二进制的 launcher 与 offline install；`setup --demo` 的 public MCP smoke 因 #195 已由人工明确接受为非核心已知限制，唯一对应测试必须显式 skip，其他 15 项必须通过。没有 Intel 主机时，x64 只证明 Mach-O、签名、包内容、`os/cpu`、离线 lock 和 checksum 契约，不构成原生执行证据。

本地构建与离线安装说明见 [`npm/README.md`](./npm/README.md)。构建脚本只写本地产物，不执行 publish 或 upload。

## 设计不变量

实现必须遵守 [`technical-design.md`](./technical-design.md) 的核心不变量。M1–M3 契约尤其禁止重新引入 Workspace 路由、Task 请求中的 Space 路由、Space 偏好排序、裸 query 自动注入、文本 TaskSignal 冒充 Graph edge，或让未确认 Candidate 进入自动注入。
