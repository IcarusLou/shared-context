interface:
  display_name: "Shared Context"
  short_description: "Run context workflow only after trusted activation"
  default_prompt: "Use $shared-context only when this session contains the trusted activation marker."
policy:
  allow_implicit_invocation: true
