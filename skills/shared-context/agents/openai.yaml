interface:
  display_name: "Shared Context"
  short_description: "Maintain Task Intent, signals, and verified references"
  default_prompt: "Use $shared-context to keep Task Intent, signals, and verified engineering references current."
