interface:
  display_name: "Shared Context"
  short_description: "Maintain Task Intent and engineering checkpoints"
  default_prompt: "Use $shared-context to keep Task Intent and signals current, explicitly checkpoint important Claims and Unknowns, query historical Context around an Artifact when needed, and record verified engineering references."
