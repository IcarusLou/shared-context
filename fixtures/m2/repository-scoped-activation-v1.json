{
  "schema": "sctx.repository-scoped-activation.acceptance.v1",
  "activation_marker_template": "<shared-context-active external_session_id=\"{host_session_id}\">Shared Context is authorized for this session. Before substantive work, call task_intent_update with agent_kind \"{agent_kind}\" and external_session_id \"{host_session_id}\" (copy it verbatim; never invent one).</shared-context-active>",
  "activation_marker_max_bytes": 512,
  "wire": {
    "codex_enabled_session_start": {
      "hookSpecificOutput": {
        "hookEventName": "SessionStart",
        "additionalContext": "{activation_marker}"
      }
    },
    "cursor_enabled_session_start": {
      "additional_context": "{activation_marker}"
    },
    "neutral": {}
  },
  "enabled_without_active_task": {
    "post_tool": "Shared Context: no ActiveTask exists. Call task_intent_update for this substantive task before continuing.",
    "pre_compact": "Shared Context PreCompact: no ActiveTask exists. Continue coding normally; use $shared-context and task_intent_update before checkpointing.",
    "turn_stop": "Shared Context TurnStop: no ActiveTask exists. Continue coding normally; use $shared-context and task_intent_update before checkpointing."
  },
  "disabled_business_residue": {
    "runtime_files": 0,
    "report_files": 0,
    "knowledge_commits_delta": 0
  }
}
