# @bytedance-dev/shared-context-darwin-x64

Signed macOS x64 binary package for `@bytedance-dev/shared-context`. The binary and checksum are
added only by the packaging builder from an explicit, already-signed input path.
