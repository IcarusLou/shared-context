{
  "name": "shared-context-npm-packaging",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:local": "node scripts/install-local.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
